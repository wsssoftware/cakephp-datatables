{#
Accessible functions parameters:
     - row (node) - TR row element that has just been created.
     - data (array, object) - Raw data source (array or object) for this row.
       the rendered data. The information given here is the original and unmodified data from the data source.
     - dataIndex (integer) - The index of the row in DataTables' internal storage..
     - cells (node[]) - The cells for the column.
#}