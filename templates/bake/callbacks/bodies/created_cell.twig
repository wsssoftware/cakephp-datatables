/**
 * {{ callback }} callback
 *
 * The content of this file will be putted inside of DataTables callback function {{ callbackFunction }}(...).
 * @link https://datatables.net/reference/option/columns.createdCell
 *
 * To use a var that was set on params, put var name inside or double parentheses. Eg.: '{{ varName }}'.
 * @link https://twig.symfony.com/doc/3.x/templates.html#variables to learn more about variables usage.
 *
 * @var {jQuery|HTMLElement} cell The TD node that has been created.
 * @var {any} cellData Cell data. If you use columns.render to modify the data, use $(cell).html() to get and modify
 *      the rendered data. The information given here is the original and unmodified data from the data source.
 * @var {any} rowData Data source object / array for the whole row.
 * @var {integer} rowIndex DataTables internal index for the row.
 * @var {integer} colIndex DataTables internal index for the column.
 */